# Optional TTS dependencies (install only when testing TTS backends)
# Do NOT install these in base or Colab by default.
# Official PyPI packages for the current REAL backends.
qwen-tts
chatterbox-tts
# Lightweight fallback (FALLBACK mode)
pyttsx3
# Audio I/O / processing
soundfile
scipy
numpy
# NOTE: Both REAL backends use torch. In Colab, keep the runtime's CUDA-enabled
# PyTorch unless a clean runtime requires an explicit compatible torch install.
