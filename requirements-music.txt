# Optional Music generation dependencies (install for MusicGen / Audiocraft backends)
# Prefer Audiocraft (Meta MusicGen) when available
audiocraft
soundfile
scipy
numpy
huggingface_hub

# Note: Audiocraft requires PyTorch; install a CUDA wheel into the music venv before pip installing audiocraft
# Example (inside venv): python -m pip install torch -f https://download.pytorch.org/whl/cu118/torch_stable.html
# Do NOT install torch into the global Colab kernel; the orchestration installs per-venv when needed.
